# راهنمای Context Menu (منوی راست کلیک)

## 📋 قابلیت جدید: مدیریت کلمات با راست کلیک

### چرا این قابلیت؟
وقتی در حال ضبط تایمینگ هستید و متوجه می‌شوید که برخی کلمات در بخش تکراری با ریتم متفاوتی خوانده شده‌اند، می‌توانید بدون دست زدن به کد، آن کلمات را به انتهای لیست اضافه کرده و دوباره با تایمینگ صحیح ضبط کنید.

## 🎯 نحوه استفاده

### 1️⃣ راست کلیک روی کلمه
- در **حالت Record** روی هر کلمه‌ای راست کلیک کنید
- منوی زیر ظاهر می‌شود:

```
┌─────────────────────────────────┐
│ 📋 کپی به انتها (C)             │
│ 📄 کپی چند کلمه به انتها        │
│ ⏩ پرش به این کلمه              │
│ ❌ بستن                         │
└─────────────────────────────────┘
```

### 2️⃣ گزینه‌های منو

#### 📋 کپی به انتها (C)
- کلمه انتخاب شده را به انتهای لیست کپی می‌کند
- با علامت 🔁 نشان داده می‌شود
- کلید میانبر: **C**

**مثال:**
```
وضعیت قبل:
El Shaddai El Shaddai...

[راست کلیک روی "El" → کپی به انتها]

وضعیت بعد:
El Shaddai El Shaddai... 
...
🔁 El  ← این کلمه اضافه شده
```

#### 📄 کپی چند کلمه به انتها
- چند کلمه متوالی از نقطه انتخاب شده را کپی می‌کند
- یک دیالوگ باز می‌شود: "چند کلمه از این نقطه کپی شود؟"
- مثلاً عدد 5 را وارد کنید تا 5 کلمه بعدی کپی شود

**مثال:**
```
[راست کلیک روی "Lord" → کپی چند کلمه → ورود عدد 3]

کپی می‌شود: Lord God Most High
به انتها اضافه می‌شود: 🔁 Lord 🔁 God 🔁 Most 🔁 High
```

#### ⏩ پرش به این کلمه
- به کلمه انتخاب شده می‌پرد (برای ادامه ضبط از آن نقطه)
- اگر تایمینگ دارد، صدا به زمان آن کلمه می‌رود

#### ❌ بستن
- منو را می‌بندد

### 3️⃣ کلید میانبر C
بعد از راست کلیک روی یک کلمه، می‌توانید کلید **C** را فشار دهید تا سریع کپی شود:

```
1. راست کلیک روی "Shaddai"
2. فشار دادن C
3. کلمه به انتها اضافه می‌شود ✅
```

## 🔧 فرآیند کار

### سناریو 1: یک کلمه تکراری
```
1. همه کلمات را با Space ضبط کردید
2. متوجه شدید "Alleluia" در Chorus سریع‌تر خوانده می‌شود
3. راست کلیک روی "Alleluia" → کپی به انتها (یا فشار C)
4. کلمه به انتها اضافه می‌شود: 🔁 Alleluia
5. موزیک را پلی کنید
6. وقتی به Chorus رسید، روی 🔁 Alleluia بودید
7. با Space تایمینگ صحیح را ضبط کنید
```

### سناریو 2: یک بخش کامل
```
1. Verse 1 را کامل ضبط کردید
2. Verse 2 دقیقاً متن Verse 1 است ولی سریع‌تر
3. راست کلیک روی اولین کلمه Verse 1
4. انتخاب: کپی چند کلمه → ورود تعداد (مثلاً 20)
5. 20 کلمه به انتها کپی می‌شود
6. موزیک را در Verse 2 پلی کنید
7. با Space همه را دوباره ضبط کنید
```

## 🚫 محدودیت‌ها

### Context menu فقط در حالت Record فعال است
```javascript
if (currentMode === 'playback') {
    console.log('⚠️ Context menu فقط در حالت record فعال است');
    return;
}
```

## 🎨 ظاهر Context Menu

### استایل
- پس‌زمینه سفید با border آبی (#667eea)
- Border-radius: 10px (گوشه‌های گرد)
- Shadow برای جلوه 3D
- Hover effect: پس‌زمینه خاکستری (#f0f0f0)

### آیکون‌ها
- 📋 برای کپی
- 📄 برای کپی چند کلمه
- ⏩ برای پرش
- ❌ برای بستن

## 💻 جزئیات تکنیکال

### متغیرها
```javascript
let selectedWordIndex = null;  // ایندکس کلمه راست کلیک شده
```

### توابع اصلی
```javascript
showContextMenu(x, y, word)      // نمایش منو در مختصات
copyWordToEnd()                  // کپی یک کلمه
copyMultipleToEnd()              // کپی چند کلمه
jumpToWord()                     // پرش به کلمه
closeContextMenu()               // بستن منو
```

### Event Listeners
```javascript
// راست کلیک روی کلمه
wordSpan.addEventListener('contextmenu', function(e) {
    e.preventDefault();
    selectedWordIndex = idx;
    showContextMenu(e.clientX, e.clientY, word);
});

// بستن منو با کلیک خارج از آن
document.body.addEventListener('click', (e) => {
    const contextMenu = document.getElementById('context-menu');
    if (contextMenu && !contextMenu.contains(e.target)) {
        contextMenu.classList.remove('show');
    }
});

// کلید C
case 'c':
case 'C':
    if (selectedWordIndex !== null) {
        copyWordToEnd();
    }
```

## 📊 ساختار داده کپی شده

```javascript
const newWord = {
    word: wordData.word,           // متن کلمه
    globalIndex: allWords.length,  // ایندکس جدید
    isRepeatSection: true,         // علامت تکراری 🔁
    copiedFrom: selectedWordIndex  // ایندکس منبع
};
```

## 🐛 عیب‌یابی

### اگر Context Menu باز نشد:
1. مطمئن شوید در حالت **Record** هستید (نه Playback)
2. Console را باز کنید (F12) و پیام‌ها را چک کنید
3. بررسی کنید که روی کلمه کلیک کرده‌اید (نه فضای خالی)

### اگر کلید C کار نکرد:
1. ابتدا باید روی یک کلمه راست کلیک کنید
2. سپس C را فشار دهید (بدون بستن منو)
3. اگر `selectedWordIndex = null` باشد، C کاری نمی‌کند

### لاگ‌های مفید در Console:
```
📋 Context menu باز شد برای: Shaddai
✅ کلمه به انتها کپی شد: Shaddai
✅ 5 کلمه به انتها کپی شد
⏩ پرش به کلمه با index: 12
⚠️ هیچ کلمه‌ای انتخاب نشده
```

## 🎯 نکات کاربردی

1. **استفاده از Repeat Section (S) vs Context Menu:**
   - S: برای تکرار بخش‌های پایانی (مثلاً 4 پاراگراف آخر)
   - Context Menu: برای تکرار هر کلمه/بخش دلخواه از وسط متن

2. **ترکیب با سایر کلیدها:**
   ```
   راست کلیک + C = کپی سریع
   راست کلیک + J = پرش و ادامه ضبط
   ```

3. **مدیریت تکرارهای پیچیده:**
   - Bridge یکبار سریع، یکبار آهسته
   - Chorus در Verse 1 آهسته، در Verse 3 سریع
   - کلمات یکسان با تلفظ متفاوت

## ✅ چک‌لیست تست

- [ ] راست کلیک روی کلمه → منو ظاهر شد
- [ ] کپی به انتها → کلمه با 🔁 اضافه شد
- [ ] کپی چند کلمه → دیالوگ باز شد، 3 کلمه کپی شد
- [ ] پرش به کلمه → currentWordIndex تغییر کرد
- [ ] کلید C → کلمه انتخاب شده کپی شد
- [ ] کلیک خارج از منو → منو بسته شد
- [ ] بستن منو با دکمه ❌ → منو بسته شد
- [ ] حالت Playback → منو ظاهر نشد (disabled)

## 📖 مثال عملی کامل

```
موزیک: "El Shaddai"
مشکل: در Bridge، "Lord God" دو بار خوانده می‌شود:
  - بار اول: آهسته و با فاصله
  - بار دوم: سریع و متصل

راه حل:
1. ابتدا "Lord God" را با تایمینگ بار اول ضبط کنید
2. موزیک را Stop کنید (حالت Record)
3. راست کلیک روی "Lord"
4. انتخاب: کپی چند کلمه → ورود 2
5. در انتها اضافه می‌شود: 🔁 Lord 🔁 God
6. موزیک را تا Bridge بار دوم Fast Forward کنید
7. پلی کنید، وقتی "Lord God" سریع خوانده شد
8. با Space روی 🔁 Lord و 🔁 God تایمینگ جدید را ضبط کنید

نتیجه JSON:
{
  "words": [
    {"word": "Lord", "start": 10.5, "end": 11.0},  // بار اول آهسته
    {"word": "God", "start": 11.2, "end": 11.8},
    ...
    {"word": "Lord", "start": 45.1, "end": 45.3},  // بار دوم سریع
    {"word": "God", "start": 45.3, "end": 45.6}
  ]
}
```

---

## 🚀 بعدی چیه؟

1. ✅ Context Menu کامل شد
2. ⏳ تست با موزیک واقعی
3. ⏳ یکپارچه‌سازی با React Component
4. ⏳ ذخیره در localStorage
5. ⏳ Import/Export JSON
6. ⏳ Deploy برای همه 364 آهنگ

---

**توسعه‌دهنده:** GitHub Copilot  
**تاریخ:** 25 اکتبر 2025  
**فایل:** test-timing-recorder.html  
**نسخه:** 2.5 (با Context Menu)
