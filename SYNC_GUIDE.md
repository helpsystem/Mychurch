# 🔄 راهنمای همگام‌سازی با سرور

این سیستم همگام‌سازی خودکار، تمام فایل‌های static را با سرور production به‌روز نگه می‌دارد.

## 📋 دستورات

### 1️⃣ **همگام‌سازی دستی (یک‌بار)**
فایل‌هایی که در 24 ساعت گذشته تغییر کرده‌اند را آپلود می‌کند:

```bash
npm run sync
```

**استفاده:**
- بعد از تولید عکس‌های جدید
- بعد از اضافه کردن فایل‌های audio
- بعد از ویرایش documents

---

### 2️⃣ **حالت مانیتورینگ (خودکار)** ⭐ پیشنهادی
به محض ایجاد یا تغییر هر فایل، خودکار آپلود می‌شود:

```bash
npm run sync:watch
```

**این حالت را در یک Terminal جداگانه باز نگه دارید!**

**مزایا:**
- ✅ Real-time sync
- ✅ نگرانی از فراموشی آپلود نداری
- ✅ همیشه سرور به‌روز است

---

### 3️⃣ **آپلود یک فایل خاص**
```bash
npm run sync:file public/images/new-photo.jpg
```

---

## 📁 پوشه‌های تحت نظر

سیستم این پوشه‌ها را مانیتور می‌کند:

- ✅ `public/audio/` - فایل‌های صوتی
- ✅ `public/images/` - تصاویر
- ✅ `public/assets/` - دارایی‌های عمومی
- ✅ `public/documents/` - اسناد PDF
- ✅ `public/data/` - فایل‌های JSON
- ✅ `public/worship/` - محتوای عبادت
- ✅ `public/generated-images/` - تصاویر AI
- ✅ `public/bible-timings/` - فایل‌های timing

---

## 🎯 سناریوهای کاربردی

### سناریو 1: تولید عکس AI
```bash
# Terminal 1: مانیتورینگ را روشن کن
npm run sync:watch

# Terminal 2: عکس تولید کن
node scripts/generate-ai-images.js

# ✅ عکس خودکار به سرور می‌رود!
```

---

### سناریو 2: اضافه کردن فایل صوتی
```bash
# فایل جدید را کپی کن
cp new-audio.mp3 public/audio/bible/

# آپلود دستی
npm run sync:file public/audio/bible/new-audio.mp3
```

---

### سناریو 3: همگام‌سازی روزانه
```bash
# صبح روز بعد
npm run sync

# همه تغییرات دیروز آپلود می‌شود
```

---

## ⚙️ تنظیمات

برای تغییر سرور، فایل `sync-to-server.js` را ویرایش کن:

```javascript
const SERVER_CONFIG = {
  host: '195.250.25.185',
  user: 'root',
  password: 'jIVeuzsrkoWPkhUY',
  remotePath: '/root/Mychurch/public/'
};
```

---

## 🚨 نکات مهم

### ✅ **امن:**
- فایل‌های حذف شده روی سرور حذف نمی‌شوند (ایمن)
- فقط آپلود می‌کند، هیچ چیز را پاک نمی‌کند

### ⚡ **سریع:**
- استفاده از SCP با فشرده‌سازی
- فقط فایل‌های تغییر یافته آپلود می‌شوند

### 🎯 **هوشمند:**
- `node_modules/`, `.git/`, `dist/` ignore می‌شوند
- فایل‌های temporary نادیده گرفته می‌شوند

---

## 📊 مثال خروجی

```
═══════════════════════════════════════════════════════════════
   🔄 شروع همگام‌سازی با سرور
═══════════════════════════════════════════════════════════════

🔍 اسکن فایل‌های تغییر یافته...

📊 5 فایل تغییر یافته پیدا شد:

📤 آپلود: images/new-photo.jpg (2.5 MB)
✅ موفق: images/new-photo.jpg

📤 آپلود: audio/bible/GEN_1_fa.mp3 (3.2 MB)
✅ موفق: audio/bible/GEN_1_fa.mp3

═══════════════════════════════════════════════════════════════
   ✅ همگام‌سازی تکمیل شد!
═══════════════════════════════════════════════════════════════

📊 آمار:
   ✅ موفق: 5 فایل
   ❌ ناموفق: 0 فایل
   📦 حجم کل: 15.7 MB
```

---

## 🔧 عیب‌یابی

### مشکل: "pscp not found"
```bash
# نصب PuTTY (شامل pscp)
choco install putty
```

### مشکل: "Connection refused"
```bash
# چک کردن اتصال
ping 195.250.25.185

# تست SSH
ssh root@195.250.25.185
```

### مشکل: فایل آپلود نمیشه
```bash
# چک کردن مسیر
node sync-to-server.js --file public/your-file.jpg

# لاگ دقیق
set DEBUG=* && npm run sync
```

---

## 🚀 Best Practices

### ✅ **توصیه شده:**
1. همیشه `npm run sync:watch` در background داشته باش
2. در پایان روز یک بار `npm run sync` بزن (اطمینان)
3. قبل از deploy production، `npm run sync` اجرا کن

### ❌ **توصیه نمیشه:**
1. آپلود دستی با FTP/SCP (استفاده از این سیستم!)
2. فراموش کردن همگام‌سازی
3. تغییر مستقیم فایل‌ها روی سرور

---

## 📞 پشتیبانی

اگر مشکلی داشتی:
1. لاگ‌ها را چک کن
2. فایل `sync-to-server.js` را بررسی کن
3. اتصال سرور را تست کن

**تمام! حالا همیشه sync هستی! 🎉**
