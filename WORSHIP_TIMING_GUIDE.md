# 🎵 راهنمای کامل سیستم ضبط تایمینگ سرودهای پرستشی

## 📋 خلاصه تغییرات

### ✅ مشکلات حل شده:
1. **خطای 500 در ورود ادمین** - اصلاح endpoint از `/api/auth/login` به `/api/auth/admin-login`
2. **دکمه Timing Recorder** - اضافه شدن به صفحه Worship Songs با دسترسی محدود

---

## 🔐 اطلاعات ورود ادمین

**ایمیل:** `help.system@ymail.com`  
**رمز عبور:** `Samyar@1989`

**لینک ورود ادمین:** http://localhost:5173/#/admin/login

---

## 🎯 نحوه استفاده از سیستم

### مرحله 1: ورود به سیستم
1. به آدرس http://localhost:5173/#/admin/login بروید
2. ایمیل و رمز عبور بالا را وارد کنید
3. بعد از ورود، به Dashboard هدایت می‌شوید

### مرحله 2: دسترسی به صفحه سرودها
- **روش اول:** از منوی سایت به Worship Songs بروید
- **روش دوم:** مستقیم به http://localhost:5173/#/worship-songs بروید

### مرحله 3: باز کردن Timing Recorder
روی دکمه **⏱️ ضبط تایمینگ** (سمت چپ بالای صفحه) کلیک کنید

> **نکته:** این دکمه فقط برای کاربران با دسترسی‌های زیر نمایش داده می‌شود:
> - SUPER_ADMIN
> - MANAGER  
> - USER با permission "Worship"

---

## 🎹 آموزش ضبط تایمینگ

### قدم 1: بارگذاری فایل صوتی
1. روی **"انتخاب فایل صوتی"** کلیک کنید
2. فایل MP3 سرود را انتخاب کنید
3. صبر کنید تا فایل بارگذاری شود

### قدم 2: وارد کردن متن سرود
1. متن سرود را در کادر **"متن سرود"** بنویسید
2. هر کلمه را در یک خط جداگانه بنویسید
3. برای بخش‌های تکراری (Repeat)، از همان کلمات استفاده کنید

### قدم 3: ضبط تایمینگ با Space
1. روی **Play** کلیک کنید تا موسیقی شروع شود
2. هر بار که کلمه‌ای خوانده می‌شود، **Space** را فشار دهید
3. کلمه فعلی زرد رنگ می‌شود و به کلمه بعدی می‌رود

### قدم 4: استفاده از Context Menu (راست‌کلیک)
روی هر کلمه **راست‌کلیک** کنید:
- **📋 Copy to end (کپی تا آخر)** - تمام تایمینگ‌های از این کلمه تا آخر را کپی می‌کند
- **📋 Copy multiple (کپی چندتایی)** - چند کلمه را یکجا کپی می‌کند
- **➡️ Jump to word (پرش به کلمه)** - به کلمه موردنظر می‌پرید
- **✖️ Close (بستن)** - منو را می‌بندد

### قدم 5: مدیریت بخش‌های تکراری
1. وقتی به بخش تکراری رسیدید، **R** را فشار دهید
2. کلمات تکراری با **پس‌زمینه زرد** نمایش داده می‌شوند
3. با فشار Space تایمینگ‌ها کپی می‌شوند

### قدم 6: ذخیره فایل JSON
1. بعد از تمام شدن، روی **💾 Export JSON** کلیک کنید
2. فایل `song_timing.json` دانلود می‌شود

---

## ⌨️ کلیدهای میانبر

| کلید | عملکرد |
|------|--------|
| **Space** | ثبت تایمینگ کلمه فعلی |
| **R** | فعال/غیرفعال کردن حالت Repeat |
| **S** | شروع/توقف موسیقی |
| **J** | پرش به کلمه موردنظر |
| **A** | پخش خودکار (Auto-play) |
| **C** | بستن Context Menu |
| **Ctrl+Z** | Undo آخرین تایمینگ |
| **راست‌کلیک** | نمایش Context Menu |

---

## 📁 ساختار فایل JSON خروجی

```json
{
  "title": "نام سرود",
  "timings": [
    { "word": "کلمه", "time": 1.234 },
    { "word": "بعدی", "time": 2.567 }
  ]
}
```

---

## 🎬 ویدیوی صفحه Worship Songs

صفحه شامل:
- **🎥 دکمه Presentation Mode** (راست بالا)
- **⏱️ دکمه Timing Recorder** (چپ بالا) - فقط برای رهبران پرستش
- لیست تمام سرودها با عکس و نام

---

## 🔧 نکات فنی

### فایل‌های تغییر یافته:
1. **`lib/auth.ts`** - اصلاح endpoint ورود ادمین
2. **`pages/WorshipPage.tsx`** - اضافه شدن دکمه Timing Recorder
3. **`public/timing-recorder.html`** - ابزار ضبط تایمینگ

### دسترسی‌ها:
```typescript
const isWorshipLeader = user && (
  user.role === 'SUPER_ADMIN' || 
  user.role === 'MANAGER' || 
  (user.role === 'USER' && user.permissions?.includes('Worship'))
);
```

---

## 🐛 عیب‌یابی

### مشکل: دکمه Timing Recorder نمایش داده نمی‌شود
**راه حل:** 
- مطمئن شوید وارد شده‌اید
- نقش کاربر باید SUPER_ADMIN، MANAGER یا USER با permission "Worship" باشد

### مشکل: خطای 500 در ورود
**راه حل:** این مشکل حل شد. کد به `/api/auth/admin-login` درخواست می‌دهد

### مشکل: فایل صوتی بارگذاری نمی‌شود
**راه حل:** 
- فرمت فایل باید MP3 باشد
- حجم فایل نباید بیش از 50MB باشد
- مرورگر باید از HTML5 Audio API پشتیبانی کند

---

## 📞 پشتیبانی

برای مشکلات فنی یا سوالات، با تیم توسعه تماس بگیرید.

**نسخه سند:** 1.0  
**تاریخ آخرین بروزرسانی:** 2025-01-25
