---
description: Deploy Broadcast Console Improvements
---
1. Use the `git status` command to check for modified files.
// turbo
2. Stage all changes: `git add .`
3. Commit with a descriptive message: `git commit -m "feat: enhance broadcast console with bible selector improvements and worship song fixes"`
4. Push to origin: `git push origin main`
