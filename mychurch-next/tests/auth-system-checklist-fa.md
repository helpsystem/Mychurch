# چک‌لیست تأیید سیستم ورود و نقش‌های کاربری

**تاریخ:** 5 خردادماه 1402 (May 5, 2026)

## بخش 1: تست فرم ورود (Form-based Login)

### شرایط الزام:
- [ ] صفحه `/login` بدون خطا بارگذاری می‌شود
- [ ] فیلدهای ایمیل و رمز عبور نمایش داده می‌شوند
- [ ] دکمه "ورود" و "ادامه با گوگل" وجود دارند
- [ ] پیام‌های خطا برای ورود ناموفق درست نمایش داده می‌شود

### مراحل تست:
1. به `https://www.iranianchurchdc.com/login` بروید
2. ایمیل اشتباه + رمز صحیح وارد کنید
   - **نتیجه مورد انتظار:** پیام خطا: "خطا در ورود به سیستم / Login failed"
3. ایمیل صحیح + رمز اشتباه وارد کنید
   - **نتیجه مورد انتظار:** پیام خطا
4. ایمیل + رمز صحیح وارد کنید
   - **نتیجه مورد انتظار:** redirect به `/admin` یا `/profile`

---

## بخش 2: تست Google OAuth

### شرایط الزام:
- [ ] دکمه "ادامه با گوگل" کلیک‌شدنی است
- [ ] Redirect به Google صحیح انجام می‌شود
- [ ] بعد از تأیید گوگل، redirect صحیح است (نه به localhost)
- [ ] کاربر در جدول `users` ثبت می‌شود

### مراحل تست:
1. در صفحه `/login`، روی "ادامه با گوگل" کلیک کنید
2. با حساب گوگل خود تأیید کنید
3. مطمئن شوید که:
   - Redirect به `https://www.iranianchurchdc.com/api/auth/callback` است ✅
   - صفحه بعد از آن `/admin` یا `/profile` است ✅
   - کاربر در DB ذخیره شده است ✅

---

## بخش 3: تست صفحه ثبت‌نام (Signup)

### شرایط الزام:
- [ ] صفحه `/signup` بدون خطا بارگذاری می‌شود
- [ ] فیلدهای نام، ایمیل، رمز عبور وجود دارند
- [ ] ایمیل تأیید ارسال می‌شود
- [ ] کاربر جدید در جدول `users` ثبت می‌شود

### مراحل تست:
1. به `/signup` بروید
2. یک ایمیل جدید، نام، و رمز وارد کنید
3. دکمه "ثبت نام" را کلیک کنید
4. مطمئن شوید:
   - پیام موفق نمایش داده می‌شود ✅
   - ایمیل تأیید ارسال شده است ✅
   - کاربر با نقش `User` در DB وجود دارد ✅

---

## بخش 4: تست صفحه پروفایل (Profile)

### شرایط الزام:
- [ ] فقط کاربران لاگین‌شده می‌توانند دسترسی داشته باشند
- [ ] اگر لاگین نیستند، redirect به `/login` است
- [ ] اطلاعات کاربر درست نمایش داده می‌شود
- [ ] کاربر می‌تواند اطلاعات خود را ویرایش کند

### مراحل تست:
1. بدون لاگین به `/profile` بروید
   - **نتیجه مورد انتظار:** Redirect به `/login`
2. لاگین کنید و دوباره `/profile` را بازدید کنید
   - **نتیجه مورد انتظار:** نمایش اطلاعات کاربر
3. اطلاعات را ویرایش کنید
   - **نتیجه مورد انتظار:** تغییرات ذخیره می‌شوند

---

## بخش 5: تست مدیریت نقش‌ها (Admin Roles)

### شرایط الزام:
- [ ] فقط کاربران `Admin` می‌توانند به `/admin` دسترسی داشته باشند
- [ ] کاربران `User` به `/unauthorized` هدایت می‌شوند
- [ ] صفحه مدیریت نقش‌ها (`/admin/roles-management`) کار می‌کند
- [ ] مدیران می‌توانند نقش‌های کاربران را تغییر دهند

### مراحل تست:
1. با یک کاربر `User` به `/admin` بروید
   - **نتیجه مورد انتظار:** Redirect به `/unauthorized`
2. با یک کاربر `Admin` به `/admin` بروید
   - **نتیجه مورد انتظار:** صفحه داشبورد نمایش داده می‌شود
3. به `/admin/roles-management` بروید
   - **نتیجه مورد انتظار:** لیست کاربران نمایش داده می‌شود
4. نقش یک کاربر را تغییر دهید (مثلاً `User` → `Leader`)
   - **نتیجه مورد انتظار:** تغییر در DB ذخیره می‌شود

---

## بخش 6: تست صفحه وضعیت سیستم (Status Page)

### شرایط الزام:
- [ ] صفحه `/login-status` بدون خطا بارگذاری می‌شود
- [ ] تمام سیستم‌ها (`Auth`, `Database`, `OAuth`, `API`) سبز نمایش داده می‌شوند
- [ ] اگر مشکلی باشد، قرمز و با پیام خطا نمایش داده می‌شود

### مراحل تست:
1. به `/login-status` بروید
2. تمام شاخص‌ها سبز باشند ✅
3. اگر قرمز باشد، پیام خطا را بخوانید و مشکل را حل کنید

---

## بخش 7: تست نامه‌های ایمیل (Email Notifications)

### شرایط الزام:
- [ ] ایمیل تأیید ثبت‌نام ارسال می‌شود
- [ ] ایمیل دعوت (invite) ارسال می‌شود
- [ ] ایمیل‌ها هم فارسی و هم انگلیسی هستند

### مراحل تست:
1. یک کاربر جدید ثبت نام کنید
   - **نتیجه مورد انتظار:** ایمیل تأیید دریافت کنید
2. مسیر `/api/admin/invite-user` از طریق admin استفاده کنید
   - **نتیجه مورد انتظار:** ایمیل دعوت دریافت کنید

---

## بخش 8: تست دسترسی و محدودیت‌ها (Permissions)

| نقش | `/admin` | `/profile` | مدیریت نقش‌ها | ویرایش کاربران |
|-----|----------|-----------|--------------|---------------|
| Admin | ✅ | ✅ | ✅ | ✅ |
| Leader | ✅ | ✅ | ❌ | تا حدی |
| Operator | ✅ | ✅ | ❌ | ❌ |
| User | ❌ | ✅ | ❌ | ❌ |

---

## نتیجه‌گیری

- [ ] تمام موارد بالا تأیید شده‌اند
- [ ] هیچ خطای 500 یا crash نیست
- [ ] تمام پیام‌ها هم فارسی و هم انگلیسی درست هستند
- [ ] سیستم ورود و نقش‌ها کاملاً کارآمد هستند

**وضعیت نهایی:** ✅ **READY FOR DEPLOYMENT**

---

**تاریخ تأیید:** ________________  
**نام مسئول:** ________________  
**امضا:** ________________
