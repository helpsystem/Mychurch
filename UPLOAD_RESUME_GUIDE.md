# 📝 راهنمای آپلود با قابلیت Resume

## ❌ مشکل اسکریپت فعلی

اسکریپت `upload_bible_audio_webdav.cjs` قابلیت resume ندارد.
اگر:
- اینترنت قطع شود
- برق قطع شود
- اسکریپت متوقف شود (Ctrl+C)

**آپلود از اول شروع می‌شود** ❌

---

## ✅ راه حل: اسکریپت جدید با Resume

یک اسکریپت جدید ساختم: `upload_bible_audio_webdav_resume.cjs`

### مزایا:
1. ✅ ذخیره پیشرفت بعد از هر فایل موفق
2. ✅ اگر قطع شد، از جایی که بود ادامه می‌دهد
3. ✅ فایل checkpoint: `upload_checkpoint.json`
4. ✅ بعد از اتمام موفق، checkpoint خودکار پاک می‌شود

### چطور استفاده کنید:

```bash
# اجرای اسکریپت جدید
node scripts/upload_bible_audio_webdav_resume.cjs
```

اگر قطع شد، دوباره همون دستور رو اجرا کنید - از جایی که بود ادامه می‌ده! 🎯

---

## 🔍 بررسی وضعیت آپلود فعلی

اسکریپت فعلی شما **3 ساعت و 36 دقیقه** در حال اجراست.

با 1164 فایل و delay 200ms بین هر فایل:
- زمان تقریبی: 1164 × 200ms = ~4 دقیقه (فقط delay)
- با زمان آپلود هر فایل (~2-5 ثانیه): **حدود 1-2 ساعت**

**نتیجه:** به احتمال زیاد آپلود شما **کامل شده** ✅

### برای بررسی:

```bash
# چک کردن تعداد فایل‌های آپلود شده در Hidrive
# یا نگاه کردن به آخرین خروجی ترمینال
```

---

## 🎯 توصیه من:

1. **صبر کنید** تا اسکریپت فعلی تمام شود
2. بعد از اتمام، **گزارش** رو بررسی کنید: `bible_audio_upload_report.json`
3. برای دفعات بعد، از اسکریپت **جدید با resume** استفاده کنید

---

## 📊 بررسی سریع

اگر می‌خواهید ببینید چند فایل آپلود شده:

```javascript
// یک اسکریپت سریع برای چک کردن
node -e "
const fs = require('fs');
try {
  const report = JSON.parse(fs.readFileSync('./scripts/bible_audio_upload_report.json'));
  console.log('✅ تعداد فایل‌های موفق:', report.filter(r => r.success).length);
  console.log('❌ تعداد فایل‌های ناموفق:', report.filter(r => !r.success).length);
} catch(e) {
  console.log('⚠️ فایل گزارش هنوز ایجاد نشده - آپلود در حال انجام است');
}
"
```
